name: Career email tracker

on:
  schedule:
    # 04:23 / 12:23 UTC = 12:23 / 20:23 Asia/Shanghai.
    # Keep clear of GitHub Actions' top-of-hour scheduling peak.
    - cron: '23 4,12 * * *'
    # Idempotent fallback in case GitHub drops the primary schedule event.
    - cron: '43 4,12 * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: '首次部署请选择 bootstrap；之后手动补跑请选择 sync'
        required: true
        default: bootstrap
        type: choice
        options:
          - bootstrap
          - sync

permissions:
  contents: read

concurrency:
  group: career-email-tracker
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Read mailbox and update Feishu
        run: |
          if [ "$RUN_MODE" = "bootstrap" ]; then
            python -m career_tracker cloud-bootstrap
          else
            python -m career_tracker cloud-run
          fi
        env:
          RUN_MODE: ${{ github.event.inputs.mode || 'sync' }}
          MAIL_ADDRESS: ${{ secrets.MAIL_ADDRESS }}
          MAIL_AUTH_CODE: ${{ secrets.MAIL_AUTH_CODE }}
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_APP_TOKEN: ${{ secrets.FEISHU_APP_TOKEN }}
          FEISHU_NOTIFY_WEBHOOK: ${{ secrets.FEISHU_NOTIFY_WEBHOOK }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ vars.OPENAI_MODEL }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER }}
